/*
import {MOCK_DATA} from '../data/mock-data';
import {CountryService} from '../../../services/country.service';
import {it, describe, expect, inject, beforeEach, beforeEachProviders, MockApplicationRef} from 'angular2/testing';
import {AppComponent} from '../../../components/app.component';

describe('User Service Tests', () => {
    let countryService = new CountryService();
    countryService.data = MOCK_DATA;

    it('returns a list of countries', done => {
         countryService.getCountries()
                       .then(countries => {
                           expect(countries.length).toBeDefined();
                           expect(countries.length).toBe(3);
                           done();
                       })
                       .catch(error => done.fail('Error'));
    });
});
*/ 
//# sourceMappingURL=country.service.spec.js.map