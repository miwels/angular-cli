<!--
  NOTE:
  This file is not needed anymore. Angular-cli uses a proxy to Express (proxy.conf.json)
  and the index file is taken from the src/ folder (index.html)
  But we need to have the express server running (node ./bin/www)

  NOTE2:
  We don't need to define all the dependencies in the index.html, angular-cli uses
  angular-cli.json to define the dependencies
-->
<html>
  <head>
    <!-- base is important for routing -->
    <base href="/">
    <title>Angular 2 QuickStart</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
  </head>
  <!-- 3. Display the application -->
  <body>
    <my-app>Loading...</my-app>
  </body>
</html>